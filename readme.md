Run project with :
npm run dev

Project structure:
-all shared components (buttons, headers, footers, menus, dropdowns...) go in /components 
pages go in /pages, if a page has a custom component it should go in that components folder under /components (/src/pages/Homepage/components)
page components go into AppRoutes
shared assets (images,icons,svgs) go into src/assets folder
